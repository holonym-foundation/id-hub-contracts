import "hashes/blake2/blake2s" as leafHash; //NOTE: must use quantum- and length-extension- resistant hash function
def main(u32[5] address, private u32[7] creds, private u32[4] secret) -> u32[8] {
    u32[1][16] preimage = [[...address, ...creds, ...secret]];
    return leafHash(preimage);
}